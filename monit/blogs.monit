#!monit
set logfile /var/log/monit.log

check process nodejs with pidfile "/var/run/blogs.pid"
    start program = "/sbin/start blogs"
    stop program  = "/sbin/stop blogs"
    if failed port 443 protocol HTTPS
	with timeout 10 seconds
        then restart
